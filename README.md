# Lab8-Starter
<https://bekzh-an.github.io/Lab8/>

Service Workers are a tool to implement graceful degradation on the network layer. This prevents a loss of network connection from making the app completely useless. Falling back on its cache allows the app to still retain simple functions. This is an example of degrading gracefully instead of outright failing. 

![image](/pwa.png)